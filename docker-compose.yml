services:
  ecomerce-db:
    image: postgres:15
    container_name: todo-api
    environment:
      POSTGRES_USER: docker
      POSTGRES_PASSWORD: docker
      POSTGRES_DB: prisma
    ports:
      - "5434:5432"  